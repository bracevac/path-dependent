import Coercions.FCsub
import Coercions.ManySortedFC

/-!
Umbrella for the earlier targets kept for their theorems: `FCsub` (System
F-sub with explicit coercions) and the many-sorted static layer.  The
current development, Plan III, is rooted at `Coercions.DotMNF`,
`Coercions.FCdot`, and `Coercions.DotToFCdot`; see `README.md` in this
directory.
-/
