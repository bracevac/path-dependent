import Coercions.Paths.DotMNF
import Coercions.Paths.FCdot
import Coercions.Paths.DotToFCdot
import Coercions.Paths.Runtime

/-!
Paths: the path project of plan V (`plan-5-extensions.md` §4, `plan-5g-paths-stages.md`), a copy
of the vanilla line (`Coercions.DotMNF`, `Coercions.FCdot`, `Coercions.DotToFCdot`,
`Coercions.Runtime`) at the commit recorded in `BASE`, to be extended with paths, stable fields,
singleton types, the transparent self and path-keyed blocks.
-/
