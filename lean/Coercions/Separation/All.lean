import Coercions.Separation.DotMNF
import Coercions.Separation.FCdot
import Coercions.Separation.DotToFCdot
import Coercions.Separation.Runtime

/-!
Separation, the DOT way: the capture line of plan V, a copy of the vanilla line
(`Coercions.DotMNF`, `Coercions.FCdot`, `Coercions.DotToFCdot`, `Coercions.Runtime`)
at the commit recorded in `BASE`, to be extended with the capture sort of
`plan-5-extensions.md` §2 and `plan-5a-captures-note.md`.
-/
